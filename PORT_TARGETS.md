# Port targets — `tracing-appender-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `tracing-appender` that codex-rs uses, and therefore the minimum subset that `tracing-appender-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-167] (rank `254`, tier `direct`, status `To Do`)

**Cargo package name**: `tracing-appender`
**Rust module name (in `use` statements)**: `tracing_appender`

## Summary

- **2** distinct symbols imported by codex-rs
- **3** total `use` statements
- **2** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `tracing_appender::non_blocking`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-cli`, `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `cli/src/login.rs:27` (`codex-cli`) — `use tracing_appender::non_blocking`
- `tui/src/lib.rs:70` (`codex-tui`) — `use tracing_appender::non_blocking`

### `tracing_appender::non_blocking::WorkerGuard`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-cli`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `cli/src/login.rs:28` (`codex-cli`) — `use tracing_appender::non_blocking::WorkerGuard`
